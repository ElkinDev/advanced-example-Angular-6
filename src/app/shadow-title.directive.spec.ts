import { ShadowTitleDirective } from './shadow-title.directive';

describe('ShadowTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowTitleDirective();
    expect(directive).toBeTruthy();
  });
});
